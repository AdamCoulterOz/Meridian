# keel is published to GitHub Packages, not the public registry.
# NODE_AUTH_TOKEN is supplied by actions/setup-node in CI, or by `gh auth token` locally.
@adamcoulteroz:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
